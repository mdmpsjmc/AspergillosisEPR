@model AspergillosisEPR.Models.Patients.PatientRadiologyFinding
<div class="col-12 row score-form" style="margin: 0px !important">
</div>
@if (User.IsInRole("Create Role") || User.IsInRole("Admin Role"))
{
    <a href="@Url.Action("MRCScore", "Partials")" class="btn btn-success add-score-entry add-new-patient-partial" data-insert-into-class="div.score-form" style="padding: 5px">
        <i class="fa fa-plus"></i>
        <span>
            <strong>Add  MRC Score</strong>
        </span>
    </a>
}