@model AspergillosisEPR.Models.SettingsViewModels.RadiologyViewModel
<div class="modal fade new-settings-modal" tabindex="-1" role="dialog" id="new-radiology-modal">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
                <h4 class="modal-title">Add New Item</h4>
            </div>
            <div class="modal-body">
                <div class="widget-body">
                    <form asp-controller="Radiology" asp-action="Create" class="smart-form settings-form" data-ajax="true" data-ajax-method="POST" id="new-radiology-form">
                        <fieldset>
                            <div class="row">
                                <label class="input" for="Name">
                                    <i class="icon-append fa fa-info-circle"></i>
                                        <input placeholder="Enter Item Name" name="RadiologyViewModel.Name">  
                                        <input type="hidden" value="@Model.Klass" name="RadiologyViewModel.Klass">  
                                        <span class="text-danger"></span>
                                </label>
                            </div>
                        </fieldset>
                    </form>
                </div>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
                <button type="button" class="btn btn-success submit-settings-item" data-tab="radiology">SAVE</button>
            </div>
        </div>
    </div>
</div>
